# HoboFrancs Android APK Skeleton

Use Termux+Python or Chaquopy/Kivy to wrap the `hobo` CLI.

## Termux Quickstart
```bash
pkg install python git
git clone <your-repo>
cd <your-repo>
python -m hobo init
```
